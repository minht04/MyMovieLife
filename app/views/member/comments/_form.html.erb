<%= form_with(model: [post, comment], url: post_comments_path(@post) ) do |f| %>
  <%= f.text_area :body, cols:'40', rows:'5', class: "input-mysize" %><br>
  <%= f.submit class: "btn btn-outline-darl comment-submit float-right" %>
<% end %>